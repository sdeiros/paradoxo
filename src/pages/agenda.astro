---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Agenda - Paradoxo">
  <section class="page-content">
    <div class="container">
      <h1>Agenda</h1>
      <div class="content">
        <p>
          Confira nossa programação cultural com exposições, oficinas, apresentações e eventos
          que acontecem no Paradoxo.
        </p>
        
        <div class="events">
          <div class="event-card">
            <div class="event-date">
              <span class="day">15</span>
              <span class="month">FEV</span>
            </div>
            <div class="event-info">
              <h3>Exposição: Cores do Território</h3>
              <p>Abertura da exposição coletiva com artistas locais</p>
              <span class="time">18h às 21h</span>
            </div>
          </div>

          <div class="event-card">
            <div class="event-date">
              <span class="day">22</span>
              <span class="month">FEV</span>
            </div>
            <div class="event-info">
              <h3>Oficina: Pintura em Tecido</h3>
              <p>Aprenda técnicas de estamparia manual</p>
              <span class="time">14h às 17h</span>
            </div>
          </div>

          <div class="event-card">
            <div class="event-date">
              <span class="day">01</span>
              <span class="month">MAR</span>
            </div>
            <div class="event-info">
              <h3>Sarau Cultural</h3>
              <p>Noite de poesia, música e arte</p>
              <span class="time">19h às 22h</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  .page-content {
    min-height: calc(100vh - 200px);
    padding: 4rem 2rem;
    background: #f8f8f8;
  }

  .container {
    max-width: 1000px;
    margin: 0 auto;
  }

  h1 {
    font-size: clamp(2rem, 4vw, 3.5rem);
    font-weight: 800;
    color: #2b1a3d;
    margin-bottom: 2rem;
    letter-spacing: -0.02em;
  }

  .content {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .content > p {
    font-size: 1.125rem;
    line-height: 1.7;
    color: #333;
  }

  .events {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    margin-top: 1rem;
  }

  .event-card {
    background: white;
    border-radius: 12px;
    padding: 2rem;
    display: flex;
    gap: 2rem;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
    transition: transform 0.2s, box-shadow 0.2s;
  }

  .event-card:hover {
    transform: translateY(-2px);
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.12);
  }

  .event-date {
    background: #d91b7f;
    color: white;
    border-radius: 8px;
    padding: 1rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    min-width: 80px;
  }

  .day {
    font-size: 2rem;
    font-weight: 800;
    line-height: 1;
  }

  .month {
    font-size: 0.875rem;
    font-weight: 600;
    margin-top: 0.25rem;
  }

  .event-info {
    flex: 1;
  }

  .event-info h3 {
    font-size: 1.5rem;
    font-weight: 700;
    color: #2b1a3d;
    margin-bottom: 0.5rem;
  }

  .event-info p {
    font-size: 1rem;
    color: #666;
    margin-bottom: 0.75rem;
  }

  .time {
    display: inline-block;
    background: #f0f0f0;
    padding: 0.25rem 0.75rem;
    border-radius: 4px;
    font-size: 0.875rem;
    color: #666;
    font-weight: 500;
  }

  @media (max-width: 640px) {
    .event-card {
      flex-direction: column;
      gap: 1rem;
    }

    .event-date {
      flex-direction: row;
      gap: 0.5rem;
      width: 100%;
      padding: 0.75rem;
    }

    .day {
      font-size: 1.5rem;
    }
  }
</style>
